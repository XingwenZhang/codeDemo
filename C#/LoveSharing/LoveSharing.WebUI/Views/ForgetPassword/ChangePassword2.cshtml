@model LoveSharing.WebUI.Models.ChangePasswordModel

@{
    ViewBag.Title = "ChangePassword2";
}

<h2>ChangePassword2</h2>

@using (Html.BeginForm())
{
    @Html.EditorForModel()
    <input type="submit" value="Save" />
    @Html.ActionLink("Cancel and return to the Index","../Home/Index")
}